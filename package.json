{
  "name": "ai-story-generator",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm --workspace frontend run build"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}

